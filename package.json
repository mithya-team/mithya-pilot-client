{
  "name": "mithya-pilot-client",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "preinstall": "node -e \"if (!/pnpm/.test(process.env.npm_execpath || '')) { console.error('Use pnpm. npm i breaks Unistyles (bob not found).'); process.exit(1) }\"",
    "ui:init": "node scripts/ui-init.mjs",
    "ui:sync": "node scripts/ui-sync.mjs",
    "test:shadcn": "node scripts/test-shadcn-add.mjs",
    "typecheck": "pnpm --filter web typecheck && pnpm --filter native typecheck",
    "storybook:web": "pnpm --filter web storybook",
    "storybook:native": "pnpm --filter native storybook",
    "check:ui": "./scripts/check-ui-lock.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
